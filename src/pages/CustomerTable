const CustomerTable = () => {
  const customers = [
    { name: 'Jane Cooper', company: 'Microsoft', phone: '(225) 555-0118', email: 'jane@microsoft.com', country: 'United States', status: 'Active' },
    { name: 'Floyd Miles', company: 'Yahoo', phone: '(209) 555-0100', email: 'floyd@yahoo.com', country: 'Kiribati', status: 'Inactive' },
  ];

  return (
    <div className="bg-white rounded-3xl p-4 lg:p-8 mt-8 shadow-sm">
      <div className="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4">
        <div>
          <h2 className="text-xl font-bold">All Customers</h2>
          <p className="text-green-500 text-sm">Active Members</p>
        </div>
        <div className="flex flex-col sm:flex-row gap-3 w-full md:w-auto">
          <input type="text" placeholder="Search" className="bg-gray-50 border-none rounded-xl px-4 py-2 text-sm w-full" />
          <select className="bg-gray-50 border-none rounded-xl px-4 py-2 text-sm text-gray-400">
            <option>Short by: Newest</option>
          </select>
        </div>
      </div>

      <div className="overflow-x-auto">
        <table className="w-full text-left min-w-[800px]">
          <thead>
            <tr className="text-gray-400 text-sm border-b">
              <th className="pb-4 font-medium">Customer Name</th>
              <th className="pb-4 font-medium">Company</th>
              <th className="pb-4 font-medium">Phone Number</th>
              <th className="pb-4 font-medium">Email</th>
              <th className="pb-4 font-medium">Country</th>
              <th className="pb-4 font-medium text-center">Status</th>
            </tr>
          </thead>
          <tbody className="divide-y">
            {customers.map((c, i) => (
              <tr key={i} className="text-sm hover:bg-gray-50 transition-colors">
                <td className="py-5 font-semibold text-gray-700">{c.name}</td>
                <td className="py-5 text-gray-600">{c.company}</td>
                <td className="py-5 text-gray-600">{c.phone}</td>
                <td className="py-5 text-gray-600">{c.email}</td>
                <td className="py-5 text-gray-600">{c.country}</td>
                <td className="py-5 text-center">
                  <span className={`px-4 py-1 rounded-lg border text-xs font-medium ${c.status === 'Active' ? 'bg-green-100 text-green-600 border-green-200' : 'bg-red-100 text-red-600 border-red-200'}`}>
                    {c.status}
                  </span>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
};

export default CustomerTable;